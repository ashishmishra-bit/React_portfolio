import React from "react";
import { Typography } from '@material-ui/core';

const Portfolio = () => {
    return (
        <div>
         this is a Portfolio
        </div>
    );
};

export default Portfolio;
